# Backend API URL
VITE_API_URL=http://localhost:8000

# LLM settings are now configured on the backend (LlmSettings in appsettings.json).
# The WeeklySummary AI report generator calls /api/llm/chat on the backend proxy.
# The following variables are no longer used by the frontend:
# VITE_LLM_BASE_URL=http://localhost:11434/v1
# VITE_LLM_API_KEY=not-needed-for-local
# VITE_LLM_MODEL=llama3.2
